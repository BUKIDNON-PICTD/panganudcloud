version: '2'
services:
  panganudcloud:
    container_name: panganudcloud
    build:
      context: .      
      dockerfile: Dockerfile
    restart: always
    image: panganudcloud:1.041
    ports:
      - 7500:9000
    command: npm run start

